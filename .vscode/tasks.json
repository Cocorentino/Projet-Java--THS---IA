{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Nettoyer et compiler le projet Java",
      "type": "shell",
      "command": "rm -rf bin && find . -name \"*.class\" -delete && javac -d . Support/neurone/*.java Support/*.java",
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "problemMatcher": "$javac",
      "presentation": {
        "reveal": "always",
        "panel": "shared",
        "clear": true
      }
    }
  ]
}
